# Images

Put your assets in this folder:

- `photo.jpg` — profile photo (square, ~400×400). Shown in the sidebar.
- `eps_G0Gp0.jpg`, `exc_xy_7_ellipce.jpg` — paper figures referenced from `_data/publications.yml`.

Any additional figures referenced in `_data/publications.yml` under the `image:` field should go here too.
